> 在环境不可逆的约束下，把模型的概率性推理，收束为**可验证、可回滚、可审计**的行动序列。
>
> *Turn probabilistic model reasoning into verifiable, reversible, auditable action.*

mazi 是一个面向复杂真实任务的 **AI Agent Harness**：把 **模型的判断** 与 **harness 的事实** 严格分层记录的 agent 运行时。
## 序：为什么要造 mazi

5年前，我和朋友说，“总有一天，程序员会革自己的命。”

只是没想到，这一天来的这么快，而这革命却是将所有人都卷入了一个无法停止的漩涡。是 ***我们训练AI，解放智力？***  还是 ***AI训练我们，完成碳基生命到硅基生命的火炬交接***

**当AGI到来，我们到底还需要写什么代码？**
	答案不是「补智商」的代码，而是「管行为」的结构。

## 目录
- [1. 第一性原理：为什么必须有 Harness](#1-第一性原理：为什么必须有 Harness)
- [2. 架构总览：一闭环 + 三支柱 + 两回路](#2-架构总览一闭环--三支柱--两回路)
- [3. 什么是核心重要的：不可裁剪的下限](#3-什么是核心重要的不可裁剪的下限)
- [4. 什么是不变的：环境属性，不随模型变强而消失](#4-什么是不变的环境属性不随模型变强而消失)
- [5. 什么会被 Agent 进化替代：脚手架纪律](#5-什么会被-agent-进化替代脚手架纪律)
- [6. 面向 AGI 的工程纪律](#6-面向-agi-的工程纪律)
- [7. 面向用户的生产力](#7-面向用户的生产力)
- [8. 技术栈与工程规模](#8-技术栈与工程规模)
- [19. 快速开始](#9-快速开始)
- [10. 仓库结构](#10-仓库结构)
- [11. 工程文化：文档优先 + 测试先行](#11-工程文化文档优先--测试先行)
- [12. 设计文档地图](#12-设计文档地图)
- [13. Roadmap](#13-roadmap)

---

## 1第一性原理：为什么必须有 Harness

### 1.1 环境侧：四条硬约束

1. **环境部分可观测**——行动前拿不到完整信息；
2. **动作不可逆**——错误代价无法靠重新推理消除；
3. **时间跨度超越单次上下文**——任务状态无法天然驻留；
4. **目标在执行中可能漂移**——「完成」的定义需要外部锚点。

### 1.2 模型侧：三条内生局限

模型本质是「给定上下文输出下一个 token 分布」的函数——**无状态、无副作用、无权限概念**：

1. 输出是概率性的（能力提升不改变这一属性）；
2. 自我评估带系统性乐观偏差（自报置信度与真实准确率存在鸿沟）；
3. 对真实世界状态的掌握完全依赖上下文注入。

### 1.3 推论

> 四条环境约束是**环境的属性**，不随模型能力提升而消失。即使 AGI 实现，只要动作仍对真实世界产生不可逆影响，harness 就必须存在。

**根本分工**：模型负责生成假设，harness 负责让假设变得**可证伪**。模型输出 token 不是行动——模型说「我认为应该执行 A」，harness 回答「A 是否被允许、以什么权限、在什么隔离环境、执行后如何验证、失败如何回滚、全过程如何记录」。

**贯穿全局的分工判据**：

> 凡是「输出是概率分布、错误可由单步推理纠正」的能力，交给模型；
> 凡是「涉及真实世界状态变更、错误代价不可逆、需要跨时间维度保持一致性」的能力，交给 harness。

---

## 2. 架构总览：一闭环 + 三支柱 + 两回路

```
用户意图
  └─► 核心执行闭环：Goal ─► Plan ─► Execute ─► Observe ─► Reflect ─►（回到 Plan/Goal）
        │
        ├── 支柱一 · 记忆与状态管理 ────── 让闭环在时间维度上可延续
        ├── 支柱二 · 工具接地与权限边界 ── 让闭环在真实世界上可安全落地
        ├── 支柱三 · 可观测、审计与校准 ── 让闭环在事后可验证、可复盘
        │
        └─► 治理回路一（校准）：评估 → 策略/约束更新 → 再评估
            治理回路二（进化）：失败账本 → Harness 自身迭代
```

**凝练公式**：

> **Harness = 目标闭环 + 状态记忆 + 权限边界 + 审计反馈**
>
> 目标闭环负责「做对事」，权限边界负责「不越界」，状态记忆负责「在长程任务中保持一致」，审计反馈负责「持续可纠错」。

### 2.1 设计不变量

> **每一步的「判断」由模型做出，每一步的「事实」由 harness 记录。**

任何时刻停下，都能从日志重建「模型当时看到了什么、决定了什么、harness 执行了什么、环境发生了什么」。这是全部可信性的来源，也是 WebUI 的「透明即信任」原则。

### 2.2 坐标系：Goal → Task → Step

所有执行与审计都锚定在一棵 **Goal 树**上：Goal（意图/委托）→ Task（可独立验收的子目标）→ Step（最小动作：thinking / tool / observation）。两条法律是架构底线：

- **法律 1 · 归因链闭合**：任何 Goal 沿 `parent` 边回溯，必须终止于 `origin.kind ∈ { human, system }` 的根 Goal——**没有孤儿动作**；
- **法律 2 · 治理上限单调递减**：委托边 `child.permissionCeiling ≤ 上游`、`child.budget ≤ 上游预算`；切分边兄弟预算之和 ≤ 父预算。**唯一升权路径 = 人类审批**。

切分边与委托边在同一棵树上统一，用一个校验函数同时封死「拆意图绕预算」与「委托下游绕权限」。

### 2.3 分层与依赖方向

```
apps/webui   Vue 3 生产界面（对话流 / Context / 审计 / 存储 / 设置）
apps/api     NestJS + Fastify，REST + SSE，会话/工作区/审批/目录/事件
    │
packages/runtime   执行实现：Goal 编排 · 工具网关 · 事件总线 · Provider 供给 · 目录与计费
packages/provider  厂商适配：DeepSeek · pi-ai 适配器 · 模型发现 · 官方价目解析
packages/core      契约层（无实现依赖）：authz · gts · observability · permissions · usage · id
packages/libs      跨端共享类型（Goal 树投影等）
```

依赖方向单向向下：**契约层不依赖实现，实现不依赖界面**。核心契约可被独立测试、独立演进。

---

## 3. 什么是核心重要的：不可裁剪的下限

mazi 用一个残酷的方法论筛选「核心」——**删除测试**：

> 删掉它，会不会有某条不变量断裂？
> 会断的，留下；不会断的，删掉。

### 3.1 权限系统：不可裁剪 vs 可裁剪

权限系统 v2 以 **Domain / Label / Role** 三原语 + **R1–R6 六条派生规则** + 优先序为核心，执行侧是 **ToolGateway 11 阶段管线**。即便全量实现成本高，以下部分被明确判定为**安全下限**：

| 不可裁剪（核心）                             | 理由                                     |
| -------------------------------------------- | ---------------------------------------- |
| 三原语 + 六规则 + 优先序                     | 语义正确性基础；裁剪即语义错误           |
| 派生流程 + 钉版快照                          | 无钉版则策略变更不可归因                 |
| R3-hard 断流（secret 必断流）                | secret 泄漏不可接受                      |
| 自动压缩继承账本（防世代清洗绕过）           | 致命绕过                                 |
| SecretRef 句柄绑定 + generic egress 永不解析 | 句柄旁路是致命绕过                       |
| 代签器唯一构造 wire format + 签名策略校验    | 否则 L1 安全建立在解析器无歧义的假设上   |
| hard 层 + fail-closed + 默认 forbidden       | 无强制即无执行                           |
| 根层签名 + 审计只追加                        | TCB 不能自证；审计不可篡改是事后归因基础 |
| 账本域 = 上下文世代 + 账本版本 + 屏障        | 竞态可绕过条件裁决                       |
| Derived Label Overlay（三轴修正）            | workspace artifact 中继是结构性缺口      |

被归类为**可裁剪增强**的（信任阶梯、条件预授权、L1/L2 密钥、形式化验证等），裁剪后只会变「更保守、更多摩擦」，不会打开绕过路径——这是「核心 vs 增强」的判别标准：**裁剪后是变慢还是变不安全。**

### 3.2 观测：事实不可删

事件信封是 `{ rootGoalId, goalId, taskId, stepId }` 四元组坐标 + `seq` 排序权威 + `prevHash/hash` 哈希链 + `causedBy` 因果边 + `actor` 来源标注，**append-only、闭集 kind 注册**。

| 删除候选                 | 后果                                         | 裁决         |
| ------------------------ | -------------------------------------------- | ------------ |
| 四元组                   | 归因链与坐标系衔接断裂，回放无法按 Goal 聚合 | 留           |
| seq                      | 恢复完整性、全序、防篡改定位全部失据         | 留           |
| 哈希链                   | 审计退化为「日志说啥信啥」                   | 留           |
| causedBy                 | 委托闭合、审批→放行、观察→决策不可机器判   | 留           |
| 独立 decisionLog 存储    | 双真相源，与「删 children 反向字段」同理     | 不设（投影） |
| 事件 update / delete API | 审计不可篡改性被破坏                         | 不设         |

### 3.3 目标：归因与承诺不可删

三层结构、`parent` 链、法律 1/2、机械可判的 `GoalContract` 形式，删掉它们等于删掉架构哲学。

> **验收标准**：能否用一段代码或测试**机械地**判断「任务是否完成」？不能，说明目标定义还不够。

### 3.4 用「失败账本」定义核心

> 每个 harness 组件能否说清「它是因为哪个具体失败而存在的」？**说不清就是冗余。**

---

## 4. 什么是不变的：环境属性，不随模型变强而消失

模型越强，harness 里「**认知**」的部分越会被吸收；但「**治理**」的部分不会消失，反而更重要。区分标准只有一句：

> **问：这段代码服务「理解」，还是服务「承诺」？**
> 服务理解的（解析、编排、装配、生成）设计上就该被删；
> 服务承诺的（冻结、机械可判、审批锚点、递减校验、事实留存）删它们等于删架构哲学。

### 4.1 永远不变的部分

| 不变项                                                  | 为什么它不随 AGI 消失                                |
| ------------------------------------------------------- | ---------------------------------------------------- |
| 权限边界与确定性策略                                    | 不可逆性是环境属性，不是模型缺陷                     |
| 状态持久化 · checkpoint / resume                       | 跨时间一致性无法驻留在权重里                         |
| 归因链闭合 · 因果边 · 四元组                          | 责任归属需要外部坐标系                               |
| append-only · seq · 哈希链 · 完整性校验              | 事后可验证、防篡改是审计的存在前提                   |
| 脱敏义务与 untrusted 来源标记                           | 秘密外泄防线 = 环境属性                              |
| 机械可判的契约**形式**（冻结语义、预算/权限上限） | 消费者是评估器/审批人/审计员，读不到模型内部状态     |
| 审批终止于人类                                          | 后果转移是仪式，不是纠错                             |
| 闭集事件注册制                                          | closed-world：域随治理面演进增补，但绝不开放任意事件 |

一句话：**模型可以变得更聪明，但世界仍然不可逆、不可完全观测，责任仍然需要被指认。**

---

## 5. 什么会被 Agent 进化替代：脚手架纪律

这是 mazi 最重要的工程态度：**今天由 harness 补上的认知脚手架，明天模型能做到，就移除它。** Harness 应从「补智商」演进为「管行为」。

| 组成                                    | 演化预测                                         | 依据                                  |
| --------------------------------------- | ------------------------------------------------ | ------------------------------------- |
| 解析 / 切分编排（intake 管线）          | 削薄 → 删除                                     | 理解劳动被模型吸收                    |
| 契约**内容**生成                  | 模型自撰，成本趋零                               | 能力内化                              |
| 上下文装配（压缩 / 检索 / 裁剪 / 投影） | 实现被吸收，但**水位 + digest 留痕不消失** | 服务「呈现」的部分被吸收              |
| 计划与重规划编排                        | 逐步被模型接管                                   | 认知能力内化                          |
| 反思 / 自评文本生成                     | 模型自评变强，但**独立评估器仍保留**       | 行动者/评价者分离是盲区，不是能力问题 |
| 单步高可靠解析的过量留痕                | 可坍缩为标记字段                                 | 留痕必要性随单步可靠性下降            |
| schema 结构化投影的生成                 | 部分吸收                                         | 服务「呈现」                          |
| **权限 / 审计 / 状态 / 归因**     | **不变**                                   | 环境属性；见 §5                      |

这把「技术债」重新定义了：**很多 scaffolding 不是债，是「到期的脚手架」**——到期就该拆，而不是继续加。

### 5.1 删除测试（本层）

| 删除候选                                 | 后果                                                    | 裁决 |
| ---------------------------------------- | ------------------------------------------------------- | ---- |
| Goal / Task / Step 三层结构              | 归因链断裂，验收失去类型层强制                          | 留   |
| `rootGoalId` / `Goal.parent`         | 归因终点与委托链结构丢失                                | 留   |
| `permissionCeiling` / `budget`       | 越权路径打开                                            | 留   |
| Delegation 独立实体                      | **无断裂**：可由 parent 边 + 委托事件 + 字段推导  | 解散 |
| IntentSlice 独立类型                     | **无断裂**：statement 并入 Goal，解析归因由树表达 | 坍缩 |
| `children` / `siblingGroup` 反向字段 | **无断裂**：可从 parent 边派生，双真相源风险      | 不设 |

---

## 6. 面向 AGI 的工程纪律

### 6.1 可验证奖励优先于主观判断

- 能用确定性代码判断的，**绝不交给 LLM judge**：schema 合规、transaction ID、测试通过、数值范围；
- LLM judge 仅用于语义类问题，且 judge 本身需用人工标注样本校准；
- **原理**：确定性检查便宜、可重复、可解释；judge 引入二次不确定性，必须严格限定范围。

### 6.2 四层级评估 + 外部校准

span（单次工具调用）→ trace（端到端任务）→ trajectory（决策路径）→ session（跨轮一致性）。四层缺一不可：trace 失败可能源于 span 参数错误，span 全过也可能因规划不当导致 trajectory 失败。

置信度不依赖模型自报，而用多采样一致性、语义熵、logprob 聚合估计，并用 held-out 数据拟合校准器。**高不确定性动作自动降级到人工审批。**

### 6.3 两条治理回路

1. **校准回路**：评估结果流向三处——反馈给模型（反思层）、反馈给 harness 自身（失败账本）、反馈给训练管线（可验证奖励）；
2. **进化回路**：把每次生产失败当作永久输入，维护**失败分类账**——每类失败映射到 prompt 修订 / 工具 schema 调整 / hook 增加 / 评估指标新增之一，**每条规则可追溯到一个具体过往失败**。

> 最可靠的 agent，不是模型最强的，而是「模型—harness—评估」三者耦合度最高的。

### 6.4 一个形式化的信号

权限系统 v2 附带形式化目标（已证）、属性测试（已测）与红队（已审）三级保证声明，并把「哪些可裁剪、哪些不可裁剪」写成清单——**架构决策本身是可审计的**。

---

## 7. 面向用户的生产力

Harness 的价值最终要落到用户能看见、能操作、能信任的界面上。mazi 自带一套生产级 WebUI（Vue 3 + Vite），核心设计原则是 **透明即信任**：

| 能力         | 说明                                                                                                                                                                                                                                        |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 对话流       | thinking → 工具调用 → 观察 → 文本，逐 Step 嵌套、可折叠；工具显示工作路径与命令                                                                                                                                                          |
| 实时指标     | 成本、延迟、token/s、上下文占用实时呈现，而非结束后才出                                                                                                                                                                                     |
| Context 面板 | 装配投影可视化：模型「看到了什么」，与目标契约、授权、实际 trace 三方对照                                                                                                                                                                   |
| 审计面板     | 按 Goal/Task/Step 归因的 token 与金额分解、计价快照、vendor 成本拆分                                                                                                                                                                        |
| 存储面板     | 表数据浏览、全库搜索、占用统计                                                                                                                                                                                                              |
| 审批门       | 高危动作弹出人工审批（允许一次 / 本会话 / 拒绝），拒绝后优雅终止或转向，而非反复绕过                                                                                                                                                        |
| 权限档位     | 界面收敛为**只读 / 工作区写 / 完全**三档，后端接受 `text / read-only / draft / approved / autonomous` 全谱；按会话 / 工作区 / 默认三级作用域解析，且档位只是目标上限——**真正的执行判定读 v2 `EffectivePolicy`，后端权威** |
| 设置         | Provider/模型目录、API Key（本地`secrets.json`，0600，遮蔽展示）、官方价目源、通用权限                                                                                                                                                    |
| 工作区       | 项目会话与「随心聊」分离，文件工具只允许读取工作区目录内文件                                                                                                                                                                                |

对应的 API 层（NestJS + Fastify，`/api` 前缀）：会话运行、工作区、Goal 快照、审批、事件 SSE、模型目录与用量、存储、健康与配置热更新。

> **能否一句话说清「这个 agent 能读什么、能写什么、能花多少钱、能发什么」？说不清就不应上线。**

---

## 8. 技术栈与工程规模

| 维度     | 选型                                                        |
| -------- | ----------------------------------------------------------- |
| 语言     | TypeScript（monorepo project references）                   |
| 运行时   | Node.js ≥ 26                                               |
| 包管理   | pnpm ≥ 10 + Turborepo                                      |
| 后端     | NestJS 12 + Fastify 5（REST + SSE）                         |
| 前端     | Vue 3.5 + Vite 8                                            |
| 质量     | Vitest 5（单测）+ Biome 2（lint/format）+ Husky（提交钩子） |
| 模型供给 | DeepSeek 官方价目 + pi-ai 适配器 + 可插拔 Provider 抽象     |

当前仓库规模（可自行用命令复核）：

- **523** 次原子提交；
- **25** 篇、约 **8,200** 行中文设计文档（先文档、后代码）；
- **78** 个测试文件、约 **500** 个用例；
- 约 **254** 个源文件、**30k** 行 TypeScript / Vue。

---

## 9. 快速开始

**环境要求**：Node.js ≥ 26、pnpm ≥ 10。

```bash
# 1. 安装依赖
pnpm install

# 2. 准备配置（根 .env 为 API 与 WebUI 共享的配置源）
cp apps/api/.env.example .env   # 示例：API 6294 / WEBUI 6295

# 3. 启动 API（默认 http://127.0.0.1:6294，前缀 /api）
pnpm api:dev

# 4. 另开终端启动 WebUI（端口取根 .env 的 MAZI_WEBUI_PORT；示例为 6295）
pnpm ui:dev
```

**常用命令**

```bash
pnpm build        # turbo 全量构建
pnpm test         # vitest 全量单测
pnpm lint         # biome check
pnpm check        # lint + test
pnpm ui:prod      # 构建并托管 WebUI 静态产物
```

**配置模型 API Key**：优先在 WebUI「设置」中填入（写入本地 `secrets.json`，权限 0600，界面遮蔽展示）；也可用环境变量 `DEEPSEEK_API_KEY`。运行数据默认落在 `MAZI_HOME`（默认 `~/.mazi`）。

---

## 11. 仓库结构

```
mazi/
├── apps/
│   ├── api/          NestJS + Fastify 服务（REST/SSE、会话、审批、目录、存储）
│   └── webui/        Vue 3 + Vite 生产界面
├── packages/
│   ├── core/         契约层：authz v2 · Goal-Task-Step · 观测事件 · provider-core · usage
│   ├── runtime/      执行实现：Goal 编排 · ToolGateway · 事件总线 · Provider 供给 · 目录计费
│   ├── provider/     厂商适配：DeepSeek · pi-ai · 模型发现 · 官方价目解析
│   └── libs/         跨端共享类型
├── docs/             设计文档（架构 / core 契约 / runtime / web / 权限）
├── AGENT.md          Agent 协作规范：文档优先 + 测试先行 + 原子提交
└── turbo.json / pnpm-workspace.yaml
```

---

## 11. 工程文化：文档优先 + 测试先行

本项目对「怎么做」也有一套强约束，写在 [`AGENT.md`](./AGENT.md) 中，对所有人和 AI 协作者同时生效：

- **文档优先**：任何代码实现之前，先完成需求/设计/接口文档；文档是验收的唯一依据，跨模块协作以书面契约为准；
- **测试先行**：文档完成后先写测试定义验收标准，再写生产代码；测试不通过视为未完成，Bug 修复必须先补复现用例；
- **原子提交**：一次提交只包含一个完整功能点 / 一个修复 / 一次专项优化；禁止跨范围修改；
- **不过早优化**：没有 profiling 证据不得性能优化；可读性与可维护性优先；
- **技术债单独闭环**：代码异味作为独立任务登记，同样遵循「先文档、再测试、后代码」。

这套文化解释了一个数字：**523 次提交对应 25 篇设计文档**——先想清楚，再动手。

---

## 12. 设计文档地图

所有「为什么这样设计」的答案都在文档里，而不是散落在代码注释中。

| 文档                                                                      | 内容                                                          |
| ------------------------------------------------------------------------- | ------------------------------------------------------------- |
| [Agent Harness Framework](<./docs/Agent%20Harness%20Framework.md>)         | 总纲：一闭环 + 三支柱 + 两回路，第一性原理与验收标准          |
| [AHF_CORE_GTS](./docs/core/AHF_CORE_GTS.md)                                | Goal-Task-Step 坐标系、两条法律、删除测试与 AGI 演化预测      |
| [AHF_CORE_AUTHORIZATION_V2](./docs/core/AHF_CORE_AUTHORIZATION_V2.md)      | 权限系统 v2：三原语/六规则、数据流治理、TCB、最小可行子集     |
| [AHF_CORE_TOOL-GATEWAY](./docs/core/AHF_CORE_TOOL-GATEWAY.md)              | ToolGateway 11 阶段管线、审批与升权、kill switch              |
| [AHF_CORE_OBSERVATION](./docs/core/AHF_CORE_OBSERVATION.md)                | 事件信封、因果模型、哈希链、闭集分类学与装配投影              |
| [AHF_CORE_PROVIDER](./docs/core/AHF_CORE_PROVIDER.md)                      | 内容/消息/工具/流式/Usage/错误契约与厂商适配矩阵              |
| [AHF_RUNTIME_PROVIDER](./docs/AHF_RUNTIME_PROVIDER.md)                     | Provider Runtime：流式聚合、计价、重试/熔断、画像、健康、路由 |
| [模型目录与计费数据架构](./docs/runtime/模型目录与计费数据架构设计文档.md) | 模型目录事实、价目同步、账本与审计                            |
| [WebUI 设计文档](./docs/webui.md)                                          | 界面布局、交互、组件与演进路线                                |

---

## 13. Roadmap

- **用户行为追踪**：思维链与授权行为链的可视化；
- **Provider 监控面板**：延迟、能力、性价比、完成度横向对比（蛛网图），默认执行/验证/计划模型分角色配置；
- **模板功能**：prompt 模板、网页模板、输出结构模板；
- **评估深化**：独立 Reflector 与 LLM-as-Verifier 校准；
- **策略生态**：策略插件配置与第三方策略注册。

---

## 结语

> **好 harness 的标志不是复杂，而是「每一行都可被删除」**——每一行都因为过去一次具体失败而存在，也将因为未来模型能力的一次提升而优雅退场。

这是面向 AGI 时代构建 agent 的真正工程纪律，也是 mazi 想在模型浪潮里守住的东西：

**模型负责判断，harness 负责事实；模型越来越强，harness 越来越懂「什么不该交给模型」。**

*Be water, my friend.* 🌊
