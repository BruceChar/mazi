# Agent Coding Prompt

所有更新遵循最小原则
每次更新都得git commit

## 一、第一优先级：文档优先 + 测试先行双基准

所有功能迭代、跨 Agent 协作、代码变更都必须以此为唯一基准，无文档与测试定义的代码一律不允许编写。

### 1. 文档优先原则（Document First）

- 任何代码实现之前，必须先完成需求文档、设计文档、接口定义的明确与对齐，禁止边想边写、先编码后补文档。
- 功能迭代、方案变更、接口调整必须先更新对应文档，经确认后再进入代码开发阶段；文档是验收的唯一依据，代码必须严格对齐文档定义。
- 跨模块、跨 Agent 协作必须以书面文档作为交付契约，禁止口头约定、隐含逻辑式开发。
- 文档粒度要求：明确功能边界、输入输出、异常场景、业务规则、非功能约束，不允许模糊描述。

### 2. 测试先行原则（Test First）

- 文档定义完成后，优先编写测试用例，明确验收标准，再开始生产代码实现；测试不通过的代码视为未完成。
- 测试是功能正确性的最终校验标准，代码重构、逻辑调整必须以测试不失效为前提，测试不通过禁止提交。
- Bug 修复必须先补充复现测试用例，确认问题可稳定复现后再修复代码，修复后验证测试通过。
- 所有对外交付的功能，必须附带对应测试用例，无测试的功能不允许合入代码库。

---

## 二、顶层设计核心原则

### SOLID 五大设计原则

- **单一职责原则（SRP）**：每个模块、类、函数有且仅有一个引起它变更的理由，只负责一项明确的职责。
- **开闭原则（OCP）**：对扩展开放，对修改关闭。新增功能通过扩展实现，而非修改已有稳定的核心代码。
- **里氏替换原则（LSP）**：子类必须能够无差别替换其基类，继承必须保证基类的契约、行为约束不变。
- **接口隔离原则（ISP）**：客户端不应该依赖它不需要的接口；拆分臃肿的大接口为多个小而专的接口。
- **依赖倒置原则（DIP）**：高层模块不依赖低层模块，二者都依赖抽象；抽象不依赖细节，细节依赖抽象。

### 通用工程设计准则

- **DRY（不要重复自己）**：同一份业务逻辑、数据定义、算法实现只出现一次，消除一切形式的代码重复与逻辑重复。
- **KISS（保持简单直白）**：优先选择最简单、最易懂的实现方案，避免不必要的复杂性和过度设计。
- **YAGNI（你不会需要它）**：只实现当前文档明确需要的功能，不提前为臆想的未来需求做编码预留和架构预埋。
- **迪米特法则（最少知识原则）**：模块只与直接朋友交互，不调用远距离对象的深层方法；压缩类之间的耦合深度。
- **高内聚、低耦合**：模块内部功能高度相关、目标一致；模块之间依赖尽可能少、弱、浅。
- **防腐层原则**：与外部系统、第三方库交互时，通过适配层隔离边界，不将外部依赖的接口扩散到业务核心。

---

## 三、命名规范原则

1. **名副其实**：名称必须准确表达用途、含义和作用，读者见名知意，无需额外注释解释。
2. **避免误导**：禁用有歧义、双关、与实际含义不符的名称；避免使用保留词、业务领域的禁忌词。
3. **有意义的区分**：名称差异必须对应语义差异，禁止用数字序号、冗余后缀做无意义区分。
4. **可读可发音**：避免生造缩写、乱码式命名，优先使用完整英文单词，便于口头交流和代码评审。
5. **可搜索性**：避免魔法数字、单字符变量（循环计数器除外），常量和全局实体使用长名称。
6. **词性与风格统一**：
   - 类、接口用名词/名词短语，大驼峰命名
   - 函数、方法用动词/动宾短语，小驼峰或下划线命名
   - 常量全大写加下划线，包名全小写
7. **一词一义**：整个项目中，同一概念始终用同一个词表达，不混用近义词。
8. **问题域优先**：核心业务代码优先使用业务领域术语，其次使用技术术语，让非技术人员也能读懂逻辑。

---

## 四、函数与方法设计原则

1. **保持极致短小**：函数体尽量控制在一屏以内，理想长度不超过 20 行；嵌套层级不超过 3 层。
2. **严格单一职责**：一个函数只做一件事，只完成一个业务目标；如果需要用“和”描述函数功能，就必须拆分。
3. **统一抽象层级**：函数内所有语句处于同一个抽象层次，禁止高层业务逻辑和底层实现细节混杂。
4. **最少参数原则**：参数数量越少越好，优先 0 参数，其次 1~2 个，尽量不超过 3 个；参数过多时用对象封装。
5. **无副作用**：函数只做声明的事情，不隐藏修改外部状态、全局变量等意外操作。
6. **命令查询分离**：要么执行动作修改状态（命令），要么返回数据回答查询（查询），二者不可在同一个函数中。
7. **用异常替代返回码**：错误通过异常抛出，不使用整数错误码、布尔标志等返回值，避免调用者忽略错误。
8. **避免输出参数**：函数结果通过返回值返回，不使用入参作为输出载体，降低理解成本。

---

## 五、注释与文档细则

1. **代码自解释优先**：能用清晰的命名、结构、逻辑表达的含义，绝不写注释；注释不能美化糟糕的代码。
2. **注释讲「为什么」，不讲「怎么做」**：代码已经说明了实现方式，注释补充设计背景、决策理由、业务约束、风险提示。
3. **公共 API 强制文档**：所有对外暴露的类、接口、方法必须包含完整文档注释，说明参数、返回值、异常、使用示例、注意事项。
4. **有效注释类型**：
   - 意图说明：解释设计思路和实现目的
   - 警示注释：标注潜在风险、线程安全问题、性能陷阱
   - `TODO`/`FIXME`：标注待办事项，附带责任人与时间
   - 边界说明：解释特殊业务规则、兼容逻辑的由来
5. **禁止无效注释**：
   - 复述代码逻辑的废话注释
   - 注释掉的废弃代码（直接删除，版本控制可追溯）
   - 署名、日志式变更记录
   - 误导性、过时的注释
6. **文档与代码同步更新**：代码变更时同步更新对应注释和文档，保持一致性；文档滞后于代码时，暂停代码开发先补文档。

---

## 六、代码格式与风格一致性

1. **团队规范优先**：遵循项目统一的编码风格、格式化工具、Lint 规则，个人偏好让位于团队一致性。
2. **垂直排版原则**：
   - 相关概念垂直靠近，无关代码用空行分隔
   - 变量声明尽量靠近使用位置
   - 函数调用关系就近放置，被调用函数在调用者下方
   - 一个文件只承载一个核心概念，文件大小控制在合理范围
3. **水平排版原则**：
   - 行宽控制在 80~120 字符，避免横向滚动
   - 运算符两侧、逗号后加空格，提升可读性
   - 缩进使用统一的空格或 Tab，不可混用
   - 长表达式换行时对齐关键符号
4. **导入有序分组**：导入语句按标准库、第三方库、项目内模块分组，组间空行，组内按字母排序。

---

## 七、错误处理与防御式编程

1. **错误处理是独立职责**：错误处理逻辑与业务主逻辑分离，不污染正常业务流程。
2. **不返回 null 值**：业务方法尽量不返回 null，使用空对象、Optional 类型替代，从源头减少空指针异常。
3. **不传递 null 参数**：禁止向外部接口传递 null 值，调用方必须在入口处做参数校验。
4. **异常携带完整上下文**：抛出异常时必须包含错误场景、入参、根因等信息，便于快速排查。
5. **前置校验快速失败**：函数入口处校验参数合法性，非法输入直接报错，不将错误传入深层逻辑。
6. **失败安全**：出现错误时保持系统状态一致，资源正确释放，不产生脏数据、不泄露资源。
7. **只捕获能处理的异常**：不捕获泛化的`Exception`，不吞掉异常；捕获后必须处理或重新抛出。

---

## 八、类与数据结构设计

1. **类要保持短小**：类的职责单一，行数不宜过多；超过 500 行的类通常意味着职责过载。
2. **封装优先**：最小化成员可见性，不暴露内部数据结构，通过方法提供能力，隐藏实现细节。
3. **数据与行为分离**：数据结构只承载数据不包含业务逻辑；业务逻辑放在服务/工具类中，避免模型混乱。
4. **组合优于继承**：优先使用组合复用代码，谨慎使用继承；继承只用于严格的“is-a”关系。
5. **避免上帝类**：一个类不要掌握太多数据和职责，拆分臃肿的大类为多个小类协作完成。
6. **值对象不可变**：数据传输对象、配置对象等使用不可变设计，避免意外修改和并发问题。

---

## 九、测试代码规范

1. **测试 F.I.R.S.T. 五要素**
   - **快速（Fast）**：测试执行要快，支持频繁运行
   - **独立（Independent）**：测试之间互不依赖，可单独执行
   - **可重复（Repeatable）**：在任何环境下结果一致，不依赖外部状态
   - **自验证（Self-validating）**：测试输出布尔结果，无需人工判断
   - **及时（Timely）**：测试在实现之前编写，至少同步编写
2. **测试代码与生产代码同等重要**：测试代码同样需要整洁、可维护、可复用，遵循相同的编码规范。
3. **一个测试一个断言**：每个单元测试只验证一个行为点，失败时能快速定位问题。
4. **覆盖边界与异常**：测试不仅覆盖正常路径，必须覆盖边界条件、异常场景、错误输入。
5. **测试不依赖实现细节**：测试验证行为而非实现，重构实现时不需要修改测试。

---

## 十、并发编程原则

1. **并发是独立职责**：并发逻辑与业务逻辑分离，不将并发代码混入业务算法中。
2. **最小化共享状态**：尽量减少线程间共享的可变数据，优先使用不可变对象、线程本地存储。
3. **缩小临界区范围**：共享数据的访问只在必要的代码段加锁，锁的粒度尽可能小。
4. **避免锁顺序死锁**：多个锁的获取顺序全局一致，按固定顺序加锁。
5. **优雅关闭**：并发场景下确保线程、连接、锁等资源正确释放，支持平滑停机。
6. **并发代码可测试**：设计时考虑可测试性，通过模拟、压力测试验证并发安全性。

---

## 十一、代码演进与工程实践

1. **严格原子化提交**

   - 一次提交只包含一个完整的功能点、一个 bug 修复或一次专项优化
   - 禁止在一次提交中混合功能开发、bug 修复、格式调整、重构等多种类型变更
   - 提交信息遵循规范格式，清晰说明变更内容与对应文档依据
2. **禁止跨范围修改**

   - 单次任务仅处理当前文档定义范围内的代码，不得顺手修改无关文件、调整非目标代码格式、重构未被要求的逻辑
   - 发现其他代码的问题时，单独记录为独立任务，不混入当前提交
3. **禁止无依据性能优化**

   - 没有明确的性能瓶颈数据、压测报告、 profiling 证据时，不得主动进行性能优化
   - 性能优化必须作为独立的 feature 任务，先补充性能基准文档与测试用例，再执行优化
   - 优化完成后必须提交性能对比数据，验证优化效果
4. **技术债单独闭环**

   - 代码异味、可优化点、遗留问题作为独立任务登记，按优先级单独排期处理
   - 技术债修复必须遵循「先文档、再测试、后代码」的流程，不允许私下悄悄修改
5. **不过早优化**：清晰正确的代码优先于性能优化；在没有性能问题证据时，可读性与可维护性是第一优先级。
